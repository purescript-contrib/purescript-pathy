{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript": "^0.9.1",
    "rimraf": "^2.5.2",
    "bower": "^1.7.9"
  }
}
